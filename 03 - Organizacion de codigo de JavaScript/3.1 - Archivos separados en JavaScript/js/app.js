/**
 * Aquí va todo el código JavaScript
 * Siempre será importante mantener bien organizado nuestro código, 
 * puesto que generalmente vamos a ocupar muchísimos archivos, 
 * tanto de JavaScript como de Cascadas de Estilo, 
 * como de HTML, 
 * por lo que resulta muy conveniente que nosotros hagamos una separación 
 * adecuada y tengamos una carpeta exclusiva para trabajar con CSS,
 * una carpeta exclusiva para trabajar con JavaScript y una carpeta 
 * exclusiva donde estaremos agregando todas las librerías que lleguemos a 
 * utilizar, librerías que tengan que ver con CSS o librerías que tenían que 
 * ver con JavaScript.
 */